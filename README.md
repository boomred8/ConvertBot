# FileConvertBot

**FileConvertBot** — Telegram-бот для конвертации и обработки файлов.

Бот позволяет работать с файлами прямо в Telegram:

- объединять PDF
- сжимать документы
- конвертировать DOCX в PDF
- объединять изображения в один PDF

Бот написан на **Python** с использованием **aiogram 3** и модульной архитектуры.

---

## Возможности

Бот поддерживает следующие операции:

- 📄 **Конвертация DOCX → PDF**
- 📎 **Объединение нескольких PDF в один**
- 🗜 **Сжатие PDF файлов**
- 🖼 **Объединение нескольких изображений в один PDF**
- 🔄 **Автоматическая обработка файлов**
- 🧹 **Автоматическая очистка временных файлов**
- ⏳ **Ограничение времени сессии**
- 📏 **Проверка размера файлов**

После выбора режима пользователю достаточно отправить файл — бот автоматически выполнит обработку.

---

## Используемые технологии

Проект использует следующие технологии:

- **Python**
- **aiogram 3**
- **FSM (Finite State Machine)**
- **PyPDF2**
- **Pillow**
- **Ghostscript**
- **LibreOffice**
- **python-dotenv**

Зависимости устанавливаются из файла `requirements.txt`.

---

## Структура проекта

```text
ConvertBot
│
├── app
│   ├── conversion
│   │   ├── __init__.py
│   │   ├── image_to_pdf.py
│   │   └── pdf_tools.py
│   │
│   ├── handlers
│   │   ├── conversion_handler.py
│   │   ├── Profile_handler.py
│   │   └── start_handler.py
│   │
│   ├── keyboards
│   │   ├── conversion_markup.py
│   │   └── main_reply.py
│   │
│   └── utils
│       ├── cleanup.py
│       ├── human_mb.py
│       └── time_delete.py
│
├── from_user
├── .env
├── .gitignore
├── main.py
├── requirements.txt
└── run.py
```

---
## Установка
-  Клонировать репозиторий
```
git clone https://github.com/boomred8/ConvertBot.git
cd ConvertBot
```
- Установить зависимости
```
pip install -r requirements.txt
```
- Установить внешние программы
Некоторые функции используют сторонние программы.

Ghostscript
Используется для сжатия PDF.

Скачать:
https://www.ghostscript.com/

После установки добавьте программу в PATH.

LibreOffice
Используется для конвертации DOCX → PDF.

Скачать:
https://www.libreoffice.org/


## Создать .env файл
```
BOT_TOKEN=your_telegram_bot_token
```
---
## Запустить бота
```
python run.py
```
После запуска бот начинает получать сообщения через polling.

---
## Команды бота
Основная команда:
```
/start
```
Главное меню содержит:

- Конвертация 📦
- Профиль 🧑🏻
---
## Режимы работы
- DOCX → PDF
Пользователь отправляет .docx файл, бот конвертирует его в PDF через LibreOffice.

- Объединение PDF
Можно отправить несколько PDF файлов и объединить их в один документ.

- Сжатие PDF 
PDF файл обрабатывается через Ghostscript для уменьшения размера.

- Объединение изображений 
Пользователь отправляет несколько фотографий, бот собирает их в один PDF файл.

---
## Безопасность и ограничения

Для предотвращения ошибок и перегрузки системы реализованы:
- ограничение размера файлов
- ограничение количества файлов
- проверка типов файлов
- таймер сессии
- удаление временных файлов
---
## Пример бота
Telegram: 
```
@boomred_convert_bot
```
Описание бота:
>FileConvertBot — удобный инструмент для работы с файлами в Telegram: объединение PDF, сжатие PDF, конвертация DOCX в PDF.
---
## Автор
- author: boomred8
- telegram: @kabiev69
---
## Возможные улучшения
Планируемые функции:
- поддержка новых форматов
- Docker-развертывание
- поддержка облачных хранилищ
- индикатор прогресса обработки
- web-панель управления
